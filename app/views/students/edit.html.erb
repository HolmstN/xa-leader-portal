<h1>Editing student</h1>
 
<%= render 'form' %>

<%= link_to 'Delete Student', student_path(@student), method: :delete, data: { confirm: 'Are you sure you want to remove this student?' } %>
<%= link_to 'Back to Students', students_path %>